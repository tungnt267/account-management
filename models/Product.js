/**
 * @author Thanh Tung
 */
class Product {
  constructor(id, account, password, desc, client) {
    this.id = id;
    this.account = account;
    this.password = password;
    this.desc = desc;
    this.client = client;
  }
}
